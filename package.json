{
  "name": "document-summarizer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build:shared": "npm run build --workspace=@document-summarizer/database && npm run build --workspace=@document-summarizer/contracts",
    "build:apps": "npm run build --workspace=api && npm run build --workspace=worker && npm run build --workspace=web",
    "build": "npm run build:shared && npm run build:apps",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
